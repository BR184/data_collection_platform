# 进度与中间物

- 状态：进行中；收敛 BI 看板设计说明书第 5.2 节来源表，保留初版需求对接文档中的 CAT 手工条目。
- 已完成文件/变更：已提取当前修订版和初版需求文档的相关表格，确认 CAT 手工条目为 6 行四列配对数据。
- 验证：尚未重新生成 DOCX 和 PDF；完成后需复核表格内容、DOCX XML 和 Word 打开导出。
- 当前进行点：将 5.2 从平台内部字段明细改为业务指标级来源归并。

## 恢复线索

- 当前阶段：编辑独立修订版 DOCX 的 5.2 来源表。
- 恢复后首条命令：读取 `BI看板设计说明_已核验修订版.docx` 的第 5.2 表，确认 CAT 手工条目和新表行数。
- 上一份计划：`docs/plans/bi-dashboard-design-document-audit.md` 已完成并删除；当前修订版路径为 `C:/Users/admin/Documents/Codex/2026-07-23/5-2-5-1-cat-word/outputs/BI看板设计说明_已核验修订版.docx`。

## 目标与边界

- 原始需求：按初版研发看板需求文档简化 5.2 数据来源表，避免枚举数据采集平台内部字段和聚合中间字段。
- 必须保留：单元/集成测试通过率、执行/通过用例数、系统测试整体缺陷数及修复率、系统测试用例评审问题数、单元测试用例评审问题数、集成测试用例评审问题数等 CAT 手工条目。
- 明确边界：新数据采集平台没有集成测试模块；CAT 集成测试条目保留用于对接追踪，未接入前不展示，不伪造数据。
- 禁止行为：不覆盖原始待人工修订版 DOCX；不修改业务代码；不删除手工 CAT 条目。

## 约束与背景

- 初版需求文档的核心来源对象是质量指标、模块系统测试统计、系统测试分布和延期交叉表。
- 来源表按业务指标级归并；不展开 `issue_fact`、API DTO、兼容读源和每个聚合字段。
- “计算中间值（CAT）”与“CAT平台”沿用手工来源表的原文口径。

## 证据与根因

- 当前修订版 5.2 有 55 行，主要由数据采集平台内部字段和聚合字段组成，超过初版需求文档的业务粒度。
- 原始待人工修订版的 5.2 表末尾包含 6 行手工 CAT 对接条目，上一版修订时未保留。

## 方案与步骤

1. 将 5.2 说明文字改为业务指标级归并规则，并说明 CAT 条目的未接入展示边界。
2. 用少量业务分组替换数据采集平台内部字段明细。
3. 按原四列配对结构恢复 6 行手工 CAT 条目，包含重复项以保持追溯一致。
4. 用 Word 打开并导出 PDF，检查来源表版式和 DOCX XML。

## 决策记录

- 已选：以初版需求对接文档作为 5.2 的业务粒度基准；理由是用户明确要求来源表不要过细。
- 已选：手工 CAT 条目原样保留；理由是其属于用户明确维护的对接边界，不由代码事实静默删除。
- 已选：集成测试条目保留但标注未接入不展示；理由是同时满足来源追踪和新平台无集成测试模块的事实。

## 接口契约

- 无新增 API 或数据库变更。
- 文档来源表保留四列：数据、系统来源、数据、系统来源。

## 风险与假设

- CAT 当前没有在新平台接入；来源表中的 CAT 行是需求/对接清单，不代表当前 BI 已有可展示数据。
- 系统测试整体缺陷等 CAT 来源标注按用户指定的初版需求口径保留；若后续 CAT/新平台职责确认变化，应只更新来源表，不扩展内部字段明细。
