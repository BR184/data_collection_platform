# 进度与中间物

- 状态：修复候选、LDAP 和常用大数据 GitLab 只读链路均已拉起，首次全量同步和事实刷新成功；等待完成页面可见结果验收和黄金线采集。
- 已完成：否决 `d15ac937`；确认历史中无现成完整修复；从解耦前 `c093db20` 创建独立分支 `codex/golden-baseline-predecouple`，新增空目录里程碑迁移并提交为 `0ccd4b1c`。该父链继承 `3a3a4ff3` 的 `projects/users` 修复。
- 当前进行点：在 `http://localhost:30201` 验证阶段定义页的项目 9 测试阶段、项目 325 里程碑及客户问题页面的里程碑控件，并开始黄金线采集。
- 测试状态：迁移回归 2 项、Flyway烟测 19 项、Checkstyle、SpotBugs、迁移/仓库门禁、30 项打包器测试和全新包构建通过；黄金线三容器、LDAP 四容器及 GitLab 均健康，后端与 LDAP 为 `UP`、前端 HTTP 200。源库 `gitlabhq_full_import_test` 为 260 个项目、8005 个 Issue、10000 个 MR 和 66165 条标签关系；全量同步 `665/665` 成功，事实刷新 `6/6` 成功，ODS 与事实层均为 Issue 8005、MR 10000。尚未完成登录后页面验收。

## 恢复线索

- 当前阶段：修复候选登录后页面验收。
- 恢复后首条命令：`docker compose --env-file .env ps`（目录：`D:/projects/data_collection_platform_golden_deploy/qaflex-full-20260825T033623Z-61991b4f5011`）。
- 依据提交：`0ccd4b1c8671cd13a7de242cf58a53edd7e2fc4b`，父提交 `c093db20f8987efb7837ea58a4bee77ab5fd8f75`；当前解耦工作计划见 `docs/plans/decouple-god-classes-20260821.md`。

## 目标与边界

- 用户原始需求：以已部署 `qaflex-full-20260806T111653Z-8269d2b61253.tar.gz` 后的稳定修复版本建立黄金线测试，既用于验证当前解耦版本，也用于后续修改的结果对比。
- 成功标准：独立项目固定在同时包含两项已知修复、且不包含当前未提交大类拆分的可追溯版本；全新空库必须直接通过 325 里程碑目录和两个消费页面验收，再进入测试数据与结果采集。
- 明确禁止：切换、重置、清理或提交当前 `D:/projects/data_collection_platform` 的解耦工作树；不把未验证的选择性摘取代码标为稳定基线；不执行内网写入、发布或推送。

## 约束与背景

- 内网已部署包 `qaflex-full-20260806T111653Z-8269d2b61253.tar.gz` 的已知缺陷为全新部署后客户问题里程碑列表为空，以及 `projects`、`users` 表长期处于待修复状态。
- 目标黄金线数据须由本地 GitLab 生成，覆盖全部业务场景；后续以同一数据和操作步骤对比页面、接口、显示及导出结果。
- 当前主项目含同事 statistics 改造、同步游标修复和五个大类拆分的未提交改动；黄金线项目为独立 Git clone，必须完全隔离。
- 空平台验收要求：`CCProduct / 325 / MILESTONE` 目录以及客户问题页面的里程碑候选不能为空；不能把“待 GitLab 同步后自动补建”当作该验收通过。

## 证据与根因

- `3a3a4ff3`（2026-08-10，`fix(sync): 修复维表自动同步失败与反查性能`）修改同步计划、执行、租约、失败策略和索引目录，是 `projects`/`users` 自动同步修复的提交；其为 `d15ac937` 的祖先。
- `d15ac937`（2026-08-21）引入的迁移只从既有 `issue_fact` 初始化目录，运行期协调器也只在非空 325 事实发布后补建；空库没有可写入的来源值，因此该提交不能修复空平台列表。
- 本地开发库已有 1512 条 325 事实、9 个不同里程碑，对应目录为 9 组、9 成员；这解释了开发环境曾显示正常，但不能证明空平台初始化已修复。
- 独立候选空库有 2 个目录，其中项目 9 测试阶段目录为 11 组、50 成员，项目 325 里程碑目录为 0 组、0 成员；两个维度不能合并判断。
- Git 分支、reflog、stash 和不可达对象均未发现另一版完整修复；现有提交中没有满足空平台验收的基线。
- 本地 GitLab 的 `gitlabhq_full_import_test`、`gitlabhq_production`、平台 ODS 和开发库目录均给出同一组 9 个项目 325 活动里程碑：`CC2024 R3` 至 `CC2026 R3`；该一致集合构成初始数据证据。
- `0ccd4b1c` 在 `c093db20` 上只新增 `V20260825_01` 数据迁移、迁移测试、校验清单和架构契约。迁移使用稳定业务键并仅在 325 目录完全无组时写入，已有任意人工组时不补种、不覆盖。
- 全新包 `qaflex-full-20260825T033623Z-61991b4f5011` 的 manifest 为 clean `0ccd4b1c`、Flyway `20260825.01`；新鲜卷完整执行 127 个迁移后 325 目录为 9 组、9 成员。

## 方案与步骤

1. 【已完成】从远端克隆独立项目，并在项目内创建以 `d15ac937` 为根的 `codex/golden-baseline-20260821` 分支；不触碰当前解耦项目。
2. 【已完成】以 `30101/30102/15435` 生成和启动独立全新包，复现 325 目录及其消费页面为空；该候选已否决，不进入黄金线采集。
3. 【已完成】检查全部现有分支、reflog、stash 和不可达对象，确认没有可直接拉取的完整修复提交。
4. 【已完成】从四个一致数据面确认 9 个活动里程碑；在 `c093db20` 上形成 `0ccd4b1c`，用前向迁移只初始化完全空的 325 目录，并以 PostgreSQL 回归覆盖空目录与人工目录保护。
5. 【已完成】生成并启动 `30201/30202/15436` 全新包；验证 manifest、归档摘要、127 个迁移、目录值、健康和 HTTP。
6. 【进行中】已在 `30201/30202/15436` 恢复黄金线，并在 `28081/28082` 恢复 LDAP；常用 GitLab 通过 `qaflex-gitlab-cc-socket-proxy` 的 `15434` 只读连接到 `gitlabhq_full_import_test`。首次全量同步和事实刷新已成功，继续验证阶段定义与客户问题页面并开始黄金线采集。

## 决策记录

- 已选：使用独立 Git clone 隔离黄金线和解耦，符合用户熟悉的项目边界，并避免 checkout 影响未提交开发工作。
- 已否决：以 `d15ac937` 作为黄金线候选；它继承了 `projects`/`users` 修复，但 325 里程碑只在已有事实后生成，空平台验收失败。
- 已否决：从 `c093db20` 选择性摘取 `d15ac937` 中的里程碑代码来伪造“无解耦”提交；这会创造未经完整验证的新版本，不是可追溯的稳定基线。
- 已选：不摘取 `d15ac937` 混合提交；直接在解耦前 `c093db20` 上增加独立、可回归的前向迁移，形成可追溯修复提交。
- 已选：以两套本地 GitLab、ODS 和开发目录一致的 9 个活动里程碑作为版本化初始集合；只初始化完全空目录，后续范围仍由管理员目录维护，避免升级覆盖人工配置。

## 接口契约

- 本工作单元不新增或修改 HTTP API、表结构或数据格式；新增数据迁移 `V20260825_01__seed_empty_customer_milestone_catalog.sql`，只为完全空的项目 325 `MILESTONE` 目录建立 9 个组及对应精确成员。
- 黄金线记录中的场景 ID、测试数据稳定 ID、操作步骤、预期结果及截图/API/下载证据构成后续回归契约；具体格式在步骤三确定后写入机器可读契约或现有测试目录。

## 风险与假设

- `d15ac937` 的提交说明包含“decoupling”，但文件证据表明当前五个大类的实质拆分在其后；黄金线仍须通过完整场景测试证明行为稳定，不能仅凭提交信息。
- 基线数据若不覆盖组合场景，后续结果比较无法识别筛选、聚合、导出和显示的交叉回归；必须先维护覆盖矩阵再采集。
- 独立项目的工具链、依赖安装、构建和运行产物必须局限于其目录或项目允许的产物位置，不能复用或破坏当前解耦环境。
- 9 个里程碑是 2026-08-25 已核实的初始化基线，不是永久自动同步清单；GitLab 新增里程碑仍按现有业务规则由管理员决定是否纳入目录。
- 新候选尚未验证登录后页面消费及 `projects/users` 在真实同步运行中的终态；数据库初始化通过不能替代这两项端到端验收。
