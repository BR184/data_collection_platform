# 客户问题议题指派人对齐核验

## 进度与中间物

- 状态：进行中；尚未修改业务代码或数据库。
- 已完成：确认新平台正式链路为 `ods_gitlab_* -> issue_fact -> CustomerIssueRecordService/统计服务/Excel`，并确认 `issue_fact.assignee_name` 是独立事实字段。
- 当前进行：读取老平台 `CC_PRODUCT` 查询、映射与导出链路，追踪 GitLab 指派人数据在新平台 ODS、事实构建和响应投影中的完整流转。
- 验证：未执行写入型事实重建；最终结论须由代码、测试和本地可读 ODS/事实数据共同证明。
- 阻塞：内网真实 GitLab/ODS 与老平台运行库不在当前开发机；若本地数据不足，只能证明代码根因，无法给出具体缺失议题的运行时数值。

## 恢复线索

- 当前阶段：老/新平台人员字段链路对照。
- 恢复后首条命令：`rg -n -i "assignee|assign|指派人|handler" D:\projects\spidergitdata-dev\src D:\projects\data_collection_platform\backend\src`
- 上一关联工作：`docs/plans/cc-product-record-field-alignment.md`。

## 目标与边界

- 原始需求：以老平台数据为基准，确定客户问题/`CC_PRODUCT` 最新数据的“议题指派人”为空的根因，说明当前差异及修改方式。
- 成功标准：确认老平台对该字段的真实来源、选择规则、空值规则和查询范围；将其与新平台 ODS、事实、API、前端和导出路径逐项比对；给出最小且完整的修复边界。
- 禁止：不凭 GitLab 页面直觉推断；不修改用户既有工作树改动；未确认需求前不写入业务实现或执行会改写事实数据的任务。

## 约束与背景

- 老平台为字段、统计和导出业务口径的基准；新平台仅改进架构和体验。
- 正式数据源为 GitLab ODS；客户问题记录页与统计均消费 `issue_fact`。
- `handler_name` 与 `assignee_name` 为独立事实字段，当前单一规范指派身份允许构建期写为相同值，但不得在查询或导出中重新合并。
- 本次只核验项目 `325`（`CC_PRODUCT`）及其“议题指派人”字段，不扩大为其他人员字段重构。

## 证据与根因

- 待补充：老平台代码定位、GitLab 原始字段/关联表、事实构建映射、实际本地 ODS/事实样本、自动测试证据。

## 方案与步骤

1. 定位老平台运行入口、`CC_PRODUCT` 记录查询、查询构建、实体映射及 Excel 导出，证明人员字段来源与选择规则。
2. 定位新平台 ODS 表结构、镜像映射、`IssueFactBuilder`、事实表写入和客户问题 DTO/Excel 投影。
3. 查询本地可读 ODS、`issue_fact` 和目标 API，区分“ODS 未同步”“事实未重建”“事实构建取错字段”“响应/前端丢字段”。
4. 对照给出根因、精确修改点、迁移/事实重建要求和定向回归测试；如用户确认实施，再更新本计划并执行。

## 决策记录

- 已选：以代码和运行数据双证据定位，不以 GitLab 页面显示作为字段映射的唯一依据。
- 待定：具体修复是否需要扩展 ODS 镜像表字段或仅修正 `IssueFactBuilder`，取决于第 2-3 步证据。

## 接口契约

- 无新增 API 或数据格式；现有“议题指派人”应继续从独立 `assigneeName` 字段输出。

## 风险与假设

- 假设：GitLab 可能同时存在 `assignee` 单值字段与 `issue_assignees` 多值关联，当前新平台可能只读取其中一个或未将其纳入事实构建。
- 风险：老平台与新平台对“指派人”可能分别选择昵称、姓名或用户名，修复时需保持展示值对齐，而不只填充任意非空字符串。
