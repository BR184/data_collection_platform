# 重建后缺陷原因数据验证计划

## 进度与中间物

- 状态：真实重建与端到端验收完成；权威进度归档等待并发合并冲突解除。
- 已完成：当前代码运行 `498` 成功发布 18002 条事实；6 条污染事实全部清空，合法原因数量保持，真实页面验收通过。
- 当前进行：保留本计划作为冲突期间的恢复证据；不介入其他工作单元的合并选择。
- 测试状态：数据库、运行任务、CC2026R4 空数据页面、CC2026R3 有数据回归和浏览器控制台均通过。

## 恢复线索

- 当前阶段：验收完成，等待文档收尾。
- 恢复后首条命令：`git status --short --branch`；并发合并冲突解除后，将本计划的最终证据压缩写入 `docs/progress.md` 并删除本计划。
- 上一工作单元结果：`docs/progress.md` 2026-08-03 系统测试缺陷原因全集污染验证条目。

## 目标与边界

- 目标：确认本地重建已清除非法模板产生的原因全集污染，合法模板事实及 CC2026R4 页面展示正常。
- 成功标准：污染样本 `reason_category` 为空；合法模板原因保持；API 与页面不再展示同一议题的整套原因标签。
- 禁止：不修改 ODS、事实或业务数据；不修改统计、前端和其他字段。

## 约束与背景

- `issue_fact.reason_category` 只接受老平台固定修复模板；非法模板为空并进入既有非法判定。
- 事实重建只重算派生事实，不触发 GitLab 同步或修改 ODS；必须通过受权限与 CSRF 保护的平台入口提交。

## 证据与根因

- 修复前项目 9 有 6 条完整原因目录污染；修复后只读投影预计这 6 条归零，67 条合法模板原因保持。
- 根因为旧归一化入口在严格解析失败后扫描标签和全部评论正文。
- 本地后端进程于 13:45 启动，修复源码/字节码于 15:04/15:05 更新且 DevTools 重启禁用；15:38 开始的运行 `497` 因此继续执行旧归一化代码。运行成功只证明重建完成，不证明使用了新版本。
- 16:01 复核时 `sync_runs` 最新运行仍为 `497`；当前代码后端健康，但 Chrome 平台页显示未登录，不能绕过权限保护提交第二次重建。
- 用户登录后从平台受保护入口提交运行 `498`；运行于 16:10:26 开始、16:14:08 成功结束，计划/完成 6/6，写入 18002 条。ISSUE、MERGE_REQUEST、INTEGRATION_TEST 三类任务分别成功发布 8002、10000、0 条，无错误信息。
- 重建后项目 9 非空原因从 73 条降为 67 条，项目 325 保持 82 条；`31388/31461/31464/31465/31471/31473` 六条原因均为 SQL `NULL`，不存在空字符串或残余原因。
- 本地 CC2026R4 阶段目录有 7 个成员，但 `issue_fact` 没有 R4 测试阶段事实，页面正确显示“暂无数据”“已是最新”，无加载失败、页面级横向溢出或本轮控制台错误。CC2026R3 有 3391 条阶段事实，其中 25 条有合法原因；页面加载 30 个模块行且原因计数稀疏分布，证明合法数据与表格渲染未被清空或堆成原因全集。

## 方案与步骤

1. [完成] 在 Chrome 平台页建立登录态，并从受保护入口提交当前代码的事实重建。
2. [完成] 观察运行 `498` 终态，核对污染模式、合法模板数量及事实任务。
3. [完成] 验收 CC2026R4 页面，并用 CC2026R3 非空页面补充合法数据回归。
4. [阻塞] 并发合并冲突解除后更新进度文档并删除本计划。

## 决策记录

- 已选：数据库、API、页面逐层验证，不以单元测试替代真实重建验收。
- 否决：再次写入事实数据或用前端截断掩盖污染。

## 接口契约

- 无新增或修改接口、表结构和数据格式；只读现有事实与统计接口。

## 风险与假设

- 本地 CC2026R4 无测试阶段事实，不能复现内网页面的非空绝对数量；数据库污染样本和 CC2026R3 非空页面已覆盖本地可验证链路。
- 工作树存在其他工作单元产生的未解决合并冲突；不得在本任务中选择冲突分支、更新权威进度或提交推送。
