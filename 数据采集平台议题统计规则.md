1. ## 系统测试缺陷汇总：

   1. **统计范围**： CrownCAD各个版本下，对应的系统测试和回归测试标签的议题。`例如： CC2026R1项目下，包含的系统测试和回归测试标签标签：CC2026R1第一轮系统测试，CC2026R1第二轮系统测试，CC2026R1第三轮系统测试，CC2026R1回归测试。`项目对应的系统测试和回归测试标签维护位置：（管理员模式下）系统测试-议题测试阶段定义。
   2. **过滤规则**：排除“**功能屏蔽**”标签；排除“**已拒绝**”标签；排除“**建议**”标签；排除携带“**申请否决**”且处于**关闭状态**的议题；排除“**需求如此**”且处于**关闭状态**的议题。
   3. **总计属性的计算说明**：指定缺陷类型下，包含的议题缺陷总数量。注**意，如果一个议题包含多个模块标签，在针对指定缺陷类型，统计对应的缺陷总数时，多个模块下对应同一个议题的，总数按照1来计算，不会重复计算。** `例如：一个一级缺陷议题，包含工程图和草图两个模块，在统计一级缺陷的议题总数时，总数为：1.`
   4. **模块数据来源**：全量议题下，包含的所有模块信息（无论其是否属于当前项目或者当前所选择的测试阶段）。
   5. **缺陷数量计算规则**：如果当前项目或者测试阶段下，没有对应模块的对应议题，数量显示为：`0`。
   6. **各类占比的计算规则**：当分子为`0`时且分母不为`0`， 计算结果为：`0`；当分母为`0`时， 计算结果为：`/`
   7. **下载议题数据功能**： 执行定时任务后存储在mysql数据库里面的数据（**非实时数据**），同时，仍然遵循上述统计范围和过滤规则。
   8. **横向对比功能**：模块数据来源：根据指定的项目名称，获取项目下包含的模块信息，并按照模块信息统计评审数据、代码走查数据、系统测试缺陷数据等内容。
   9. **横向对比功能注意**： 目前横向对比中的数据只包含CC库的内容，不包含DGM库的内容；横向对比中关于合并请求的统计范围：**只统计目标分支是dev并且已经处于合并状态的合并请求的数据**；缺陷个数统计公式：`走查缺陷个数 = 规范性问题 + 性能问题+ 设计问题 + 逻辑问题 + 其他`；`特例：当项目名称为CC2025R1时， 统计的评审数据项目名称为CC2025R1&R2, 否则，查询评审数据的项目名称与系统测试的项目名称相同。`

2. ## 申请延期缺陷原因分析(管理员模式)

   1. **统计范围**： CrownCAD各个版本下，对应的系统测试和回归测试标签的议题；严重程度为一级缺陷或者二级缺陷或者三级缺陷。
   2. **过滤规则**：排除“功能屏蔽”标签；排除“已拒绝”标签；排除“建议”标签；排除携带“申请否决”且处于关闭状态的议题；排除“需求如此”且处于关闭状态的议题。
   3. **延期原因类型**： 技术卡点、方案卡点、资源卡点、数据异常、算法问题、机制问题、计算效率
   4. **延期原因数据来源**：根据标签内容写死在代码中。注意：当延期原因类型发生变化时，需要同步调整代码中的延期原因类型。
   5. **指定延期原因的总数计算方法**： `指定延期原因的（一级缺陷数量 + 二级缺陷数量 + 三级缺陷数量）之和`。

3. ## 系统测试非法数据

   1. **统计范围**： CrownCAD各个版本下，对应的系统测试和回归测试标签的议题。
   2. **过滤规则**：排除“**功能屏蔽**”标签；排除“**已拒绝**”标签；排除“**建议**”标签；排除携带“**申请否决**”且处于**关闭状态**的议题；排除“**需求如此**”且处于**关闭状态**的议题。
   3. **议题非法数据统计逻辑**：严重程度需要打**一级缺陷** 或 **二级缺陷** 或**三级缺陷**，否则视为非法议题；没有打**模块**标签的，视为非法议题，非法类型：**未设定严重程度**；如果议题不携带"**已修复/完成**"标签，则不会进行议题评论解析；如果打了“**已修复/完成**”标签，不按照模板回复，视为非法议题，非法类型：**未按照模板回复**；如果打了“已修复/完成”标签，缺陷原因0个或者超过1个都会被判定为非法数据，非法类型：**缺陷原因不唯一**；如果一个议题有多个议题原因评论，**则按照时间戳取最后一个**；
   4. **议题非法类型**：**未设定严重程度**；**未设定模块**；**未按照模板回复**；**缺陷原因不唯一**；
   5. **模块筛选数据来源**：全量议题的所有模块信息。
   6. **项目筛选数据来源**：系统测试-议题测试阶段定义功能中维护的项目名称。

4. ## 缺陷原因分析

   1. **统计范围**： CrownCAD各个版本下，对应的系统测试和回归测试标签的议题。
   2. **过滤规则**：排除“**功能屏蔽**”标签；排除“**已拒绝**”标签；排除“**建议**”标签；排除携带“**申请否决**”且处于**关闭状态**的议题；排除“**需求如此**”且处于**关闭状态**的议题。
   3. **模块筛选数据来源**：全量议题的所有模块信息。
   4. **缺陷原因表头数据来源**： 根据新的缺陷回复模板内容进行提取和总结后的内容。
   5. **特殊处理规则**： 由于需要兼容新旧两套模板，因此需要将旧模板中的个别缺陷原因汇总到新模板中，具体的特殊处理规则为：`新增理解偏差 = 新增理解偏差（新模板）+ 需求理解有误（旧模板）；新增需求 = 新增需求问题（老模板）；编码逻辑：业务逻辑错误 = 编码逻辑：业务逻辑错误（新模板）+ 编码逻辑错误（老模板）；编译/打包/部署问题 = 编译打包问题（新模板） + 编译打包问题（旧模板）; 机制不支持 = 机制不支持（新模板） + 算法/机制不支持`
   6. **缺陷原因占比计算方式**： 针对指定的缺陷原因的缺陷占比计算公式： **指定类型的缺陷数量总和/所有模块下所有缺陷原因个数的总和**。**注意： 分子和分母均为缺陷个数，与议题个数无关;在获取议题缺陷原因时，无论该议题是否打“已修复/完成”标签，只要按照要求填写了议题缺陷原因，均会被纳入统计。**

5. ## 议题阶段统计

   1. **统计范围**： CrownCAD各个版本下，对应的系统测试和回归测试标签的议题；严重程度为**一级缺陷**或者**二级缺陷**或者**三级缺陷**。
   2. **过滤规则**：排除“**功能屏蔽**”标签；排除“**已拒绝**”标签；排除“**建议**”标签；排除携带“**申请否决**”且处于**关闭状态**的议题；排除“**需求如此**”且处于**关闭状态**的议题。
   3. **项目筛选数据来源**：系统测试-议题测试阶段定义功能中维护的项目名称。
   4. **“轮次”属性数据来源**：项目对应的测试阶段。维护地址：（管理员模式下）系统测试-议题测试阶段定义功能。
   5. **“总计”计算方法**：`指定轮次的总计数量 = 指定轮次的（一级缺陷 + 二级缺陷 + 三级缺陷）个数之和`。

6. ## 议题查询

   1. **统计范围**：所有议题。
   2. **过滤规则**： 无过滤规则，完全按照用户在前端筛选的查询条件进行查询。

7. ## 系统测试横向对比

   1. **统计范围**：CrownCAD各个版本下，对应的需求评审和设计评审，CrownCAD和DGM代码走查数据，缺陷原因分析，系统测试缺陷汇总数据。
   2. **过滤规则**：代码走查筛选**合并请求状态=MERGED**，**合并目标分支=dev**;缺陷原因分析过滤规则同上文**4.缺陷原因分析**; 系统测试缺陷汇总的过滤规则同上文 **1.系统测试缺陷汇总**。
   3. **需求评审缺陷密度的计算说明**：需求评审缺陷数/需求文档页数(向下取整两位小数)
   4. **代码走查缺陷密度的计算说明**：代码走查缺陷合计/代码走查行数(四舍五入取整两位小数)
   5. **模块数据来源**：GitLab中CrownCAD和DGM **议题-标记**中以**模块：**开头的标签；评审中涉及的模块；（无论其是否属于当前项目或者当前所选择的测试阶段）。
   6. **各类占比的计算规则**：当分子为`0`时且分母不为`0`， 计算结果为：`0`；计算**缺陷汇总**时：当分母为`0`时， 计算结果为：`/`; 计算**评审**，**代码走查**，**缺陷原因**时：当分母为`0`时，计算结果视为：`0`。
   7. **无效数据过滤**：当整行数据为`0`或`0.0`,`/`时，判定为**无效数据行**，排除在excel外不做展示。

8. ## 客户问题统计

   1. **统计范围**：自2026.1.1之后创建的CC_Product议题。
   2. **采集频率**：每一小时执行一次全量议题的爬取任务。
   3. **过滤规则**：排除携带“**申请否决**”且处于**关闭状态**的议题；排除“**需求如此**”且处于**关闭状态**的议题。
   4. **缺陷汇总**：页面左侧下拉框使用里程碑属性进行切换。表格的模块列来自于CCproduct下的议题中出现过的模块名。
   5. **缺陷非法数据**：不遵循CrownCAD产品系统测试的议题要求（详见上文3.3**议题非法数据统计逻辑**），则被判定为缺陷非法数据。若未按照要求填写”缺陷调研模板“,则被判定为非法，非法类型：“未按照要求填写缺陷调研模板”。

9. ## 客户问题-延期问题

   1. **紧急程度标签**：需要有P1,P2,P3标签，没有打紧急标签的，响应期限按P3 72小时响应期限算。
   2. **统计范围**：统计CC_Product 项目自 2026.1.1之后创建的，仍然处于open状态的议题。
   3. **议题响应**：从议题创建开始，P1级别在24小时内，P2级别在48小时内，P3级别在72小时内，需要按照要求回复缺陷调研模板，否则，在议题上添加"**响应已延期**"标签；按照缺陷调研模板回复后， 取消"**响应已延期**"标签。
   4. **响应检测规则**：检测响应模板的表头："**# 问题调研情况说明**"是否在评论区中出现，检测到表头即认为已响应。
   5. **议题解决**：处于以下状态：已修复、申请延期、数据异常、需求如此、未复现。
   6. **解决检测规则**：超过了模板中填写的解决时间或者解决时间超过18天，且没有按照要求填写缺陷原因分析内容，则在议题上添加“解决已延期”标签。在打了“申请延期、数据异常、需求如此、未复现” 标签可以取消“解决已延期”标签，或者在打了”已修复/完成”之后，可以取消“解决已延期“标签。
   7. **解决时间的获取方法**：最长解决时间：**18天**；如果缺陷调研模板中**写了预计解决时间，且没有超过18天**，**则以调研模板中的预计解决时间为准**；如果调研模板中的预计解决时间超过18天，则按照18天来处理。
   8. 总数：统计设定了模块和未设定模块的议题，统计P1,P2,P3和未设定紧急程度的议题。

10. ## 客户问题-缺陷非法数据

    1. **紧急程度标签**：不遵循CrownCAD产品系统测试的议题要求（规则同**3.系统测试非法数据**），则被判定为缺陷非法数据。若未按照要求填写"缺陷调研模板",则被判定为非法，非法类型："**未按照要求填写缺陷调研模板**"。
    2. **要求1问题回复**：除**计划解决时间**和**一级缺陷的修改方案请模块负责人签字确认**外的问题都需要有回复内容，不满足上述条件的被判定为非法。
    3. **要求2计划解决时间**：计划解决时间有且仅能回复一个时间戳例如：**"2026年3月31日"**，**"2026.03.31"**，**"2026，03，31"**。年月日时间戳中可使用以下特殊字符分割：**".,，、/·`年月日"**，不满足上述条件的被判定为非法。
    4. **要求3负责人签字**：打了一级缺陷标签的议题需要负责人签字，二级缺陷三级缺陷无需签字，不满足上述条件的被判定为非法。

11. ## 客户问题-按功能展示缺陷数量

    1.  统计范围：CC_product 自2026.1.1以来创建的议题（open和closed都统计）
    2.  按功能筛选缺陷，展示功能的缺陷数量： 能够查看各个模块下，包含的所有功能的对应缺陷数量。

12. ## 客户问题-缺陷响应/解决效率

    1.  **统计范围**：CC_product 自2026.1.1以来创建的议题（处于open的议题）
    2.  统计规则：针对响应周期数据，只统计**已响应**（按照“缺陷调研模板”进行了回复）的数据，统计的时间单位为小时，取整数；针对解决周期数据，只统计打了**已修复/完成**的议题范围内，从创建议题到标注了**已修复/完成**的时间。 统计的时间单位为*天*，*取1位小数*，如3.5天，且支持按照版本进行查看。
    3.  响应周期：`从议题创建到“第一条回复调研模板”的时间`。
    4.  解决周期：`从议题创建到标注”已修复“的时间`。
    5.  议题创建时间：在gitlab平台中，显示的议题创建时间为GMT时间，计算效率时需+8小时转为北京时间。
    6.  如果一个议题被打了多个模块标签如：工程图 & 平台，它会分别在工程图和平台中计算效率。